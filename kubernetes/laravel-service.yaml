apiVersion: v1
kind: Service
metadata:
  name: ensat-laravel-service
spec:
  selector:
    app: laravel
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort # Allows access via NodeIP:NodePort. Use LoadBalancer for cloud.
